# Archived PROP result

This directory contains only the original 100-repetition PROP output used to report the ResNet-50 cataract experiment. It is kept as an immutable historical artifact; reruns of the cleaned script write to `results/reproduced/`.

The archived file is `PROP_ResNet50_raw_ttest_100_reps.csv`.
